#include <bits/stdc++.h>
using namespace std;
#define endl '\n'
#define int long long
#define __ ios_base::sync_with_stdio(false);cin.tie(NULL);

signed main(){__
    int T = 1,n,H,M,A;k
    while(T--){
        cin>>H>>M>>A;
        if(A == (H*M)/2)cout<<H*M<<endl;
        int gcd = gcd(H-1,M*H);

    }
    return 0;
}