#include <bits/stdc++.h>
using namespace std;
#define endl '\n'
#define int long long
#define __ ios_base::sync_with_stdio(false);cin.tie(NULL);

signed main(){__
    int T = 1,n,q;
    cin>>t;
    while(T--){
        cin>>n>>q;
        vector<int> nums(n);
        for(auto &c:nums)cin>>c;
        cin>>l>>r;
        int l,r,mx = 0; 
        for(int i = l;i<=r;i++){
            int 
        }
    }
    return 0;
}